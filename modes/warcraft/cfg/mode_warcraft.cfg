// CS2 Manager — Warcraft Classic mode base profile.
// Classic Warcraft 3 races, skills, ultimates and XP progression.
// Shared convars live in server.cfg. Panel-managed values are applied by panel_runtime.cfg.
// Isolated mode: must NOT load MatchZy, AutoReady, Retakes, Instadefuse or HeroShift.
echo "[CS2 Manager] Loading Warcraft Classic profile"

exec server.cfg
