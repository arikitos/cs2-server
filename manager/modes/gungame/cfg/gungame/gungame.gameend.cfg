// CS2 Manager: no GunGame MapChooser (ggmc) plugin is installed in this stack, so
// the stock "ggmc_change_nextmap" call is disabled — it would only log an unknown
// command. The map is owned by the panel (Server Config -> map) and applies on the
// next start / restart.
//ggmc_change_nextmap
