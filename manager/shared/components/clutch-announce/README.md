# Clutch Announce

Optional shared CounterStrikeSharp plugin. The release directory is created by `update.ps1` after a verified package is installed. Every mode declares the mount as optional, so the server remains valid when this component is absent.
